# ADR 0001: Architecture Choice

Decision: Use a monorepo with React/Vite, TypeScript Express API, shared schemas, Java grading service, docs, prompts, and Terraform skeleton.

Reason: The portfolio goal is to demonstrate full-stack workflow thinking without hiding critical boundaries.

