# ADR 0001: Architecture Choice

Decision: Use a monorepo with React/Vite, TypeScript Express API, shared schemas, Java grading service, docs, prompts, and Terraform-managed AWS infrastructure.

Reason: The project goal is to show full-stack workflow thinking without hiding critical boundaries.
