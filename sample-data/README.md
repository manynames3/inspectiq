# Sample Data

These sample images are license-safe photographic fixtures plus two generated OCR fixtures for odometer and VIN extraction. The local provider classifies them by filename so the full image-analysis workflow can run without paid model credentials; the Bedrock provider reads the raster image bytes.

See `IMAGE_CREDITS.md` for source and license notes.
